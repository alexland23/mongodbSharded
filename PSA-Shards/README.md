Run `./setup.sh` to bring up mongodb cluster. 

Modify `dbName` and `mongoVersion` in setup.sh to specify the database to shard and the specific mongo version to use.

Connect to mongos at `localhost:27017`.